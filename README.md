# TeamTaskManager
TeamTaskManager is a MERN Stack based web application used to manage projects and team tasks efficiently. It includes secure authentication, role-based access control, task assignment, and progress tracking features.  The project is built using React.js, Node.js, Express.js, and MongoDB with JWT authentication and a modern responsive ui.
